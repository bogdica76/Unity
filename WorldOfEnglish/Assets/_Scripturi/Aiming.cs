using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Aiming : MonoBehaviour {
    public float zDistance = 20.0f;
	public float posX, posY, posZ = 0.0f;


    void Update()
    {


        //var mousePos = Input.mousePosition;
		//transform.LookAt(Camera.main.ScreenToWorldPoint(new Vector3(mousePos.x + posX, mousePos.y + posY, zDistance)));
    }
}
